-- Write your PostgreSQL query statement below
select name, population, area
from world
where area >= 3000000

union

select name, population, area
from world
where population >= 25000000

-- this is faster
-- union all is faster but keeps duplicates
-- union removes duplicates
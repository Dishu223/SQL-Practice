-- Write your PostgreSQL query statement below
select s.user_id, coalesce(round(sum(case when action = 'confirmed' then 1.0 else 0 end)/count(*), 2), 0.00) as confirmation_rate
from signups s
left join confirmations c
on s.user_id = c.user_id
group by s.user_idd